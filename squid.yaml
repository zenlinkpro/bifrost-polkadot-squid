manifestVersion: subsquid.io/v0.1
name: zenlink-bifrost-polkadot-squid
version: 3
description: |-
  Zenlink Bifrost(Polkadot) Squid
build:

deploy:
  addons:
    postgres:
  processor:
    cmd: [ "node", "lib/processor" ]
  api:
    cmd: [ "npx", "squid-graphql-server"]
